import 'package:flutter/material.dart';

class EveryTimeWidget{
  EveryTimeWidget(this.icon, this.title, this.subTitle, this.content, this.color);
  IconData icon;
  String title;
  String subTitle;
  String content;
  Color color;
}